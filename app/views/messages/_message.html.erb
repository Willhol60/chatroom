<div class="<%= (message.user == current_user) ? "sent" : "received"  %>-message-box">
    <div class="sender">
        <%= message.user.email %>
        <%= message.created_at.strftime("%a %b %e at %l:%M %p") %>
    </div>
    <div class="message">
        <%= message.content %>
    </div>
</div>